# react nodejs with docker-compose

Example project on how to develop project on Docker Compose

- you can run the project using "docker-compose up"
- the web app will start on "http://localhost:8080/"
